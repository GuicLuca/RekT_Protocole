// Todo : remove this unused code ignore macro once the project will start
#![allow(unused)]

use crate::prelude::*;

mod tests;
mod error;
mod prelude;

fn main() -> Result<()> {
    println!("Hello, world!");
    Ok(())
}
